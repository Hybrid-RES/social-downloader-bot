[project]
name = "social-downloader-bot"
version = "0.1.0"
description = "Private Telegram bot for downloading social media to a home server"
requires-python = ">=3.13"
license = { text = "MIT" }

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
